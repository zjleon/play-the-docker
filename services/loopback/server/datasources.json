{
  "webDB": {
    "name": "webDB",
    "host": "${WEB_DB_HOST}",
    "port": 27017,
    "database": "admin",
    "user": "${WEB_DB_USER}",
    "password": "${WEB_DB_PASSWORD}",
    "connector": "loopback-connector-mongodb",
    "useNewUrlParser": true
  }
}
