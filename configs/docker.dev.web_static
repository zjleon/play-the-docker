FROM ubuntu:15.10
FROM node:6.2.2-wheezy

RUN mkdir /web_static
WORKDIR /web_static
ADD ./package.json ./package.json
RUN npm install
VOLUME ["/web_static/node_modules"]

ADD ./scripts/dev.web_static.initial.sh ./initial.sh
CMD ["bash", "./initial.sh"]

# set up gulp copy task
# set up gulp to watch app folder
